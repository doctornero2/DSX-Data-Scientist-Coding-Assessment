% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_mode.R
\name{calc_mode}
\alias{calc_mode}
\title{Calculate the statistical mode}
\usage{
calc_mode(x)
}
\arguments{
\item{x}{A numeric vector.}
}
\value{
A numeric vector containing the mode or all modes when there is a tie.
If every non-missing value occurs exactly once, \code{numeric(0)} is returned
because there is no statistical mode.
}
\description{
Calculates the statistical mode(s) of a numeric vector.
The mode is defined as the value or values occurring most frequently.
Missing values (\code{NA}) are ignored.
}
\details{
\code{NA} values are removed before calculating frequencies.

If multiple values have the same highest frequency, all of those values
are returned. The returned modes are sorted in ascending order.

If the input contains no non-missing values, an informative error is
generated.

A single non-missing value is considered the mode of the vector.
}
\examples{
# One mode
calc_mode(c(4, 4, 2, 3, 3, 3))

# Multiple modes (tie)
calc_mode(c(1, 1, 2, 2, 4))

# No mode
calc_mode(c(1, 2, 3, 4))

# NA values are ignored
calc_mode(c(1, 1, 2, NA, 3))

# Single value
calc_mode(10)

}
